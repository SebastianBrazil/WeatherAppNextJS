export type ICities = [
    city: {
        name: string,
        state: string,
        country: string,
        lat: string, 
        lon: string
    }
]

export type propLoc = {
    location: string;
}

export type propCoord = {
    lat: string,
    lon: string;
}